[package]
name = "slint-rust-template"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.14.1"

[build-dependencies]
slint-build = "1.14.1"

[profile.release]
codegen-units = 1
lto = true
